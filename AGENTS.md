# ResolveFlow 项目协作规则

## 1. 适用范围

- 本文件只约束 `resolveflow_agent` 目录及其子目录中的工作。
- 不得把本文件的项目规则自动应用到同级的其他学习目录。
- 当前项目路线以 `README.md` 和 `docs/02_30_day_roadmap.md` 为准。
- 如果需要实质调整架构、技术栈或 30 天路线，必须先向用户说明原因、收益、代价和对后续 Day 的影响，获得用户明确同意后才能实施。

## 2. 项目最高优先级成果与六项基础要求

### 2.0 最高优先级成果要求

在不违反安全、合规、事实真实性和用户最新明确指令的前提下，下面两项是本项目最高优先级的成果目标。架构选择、每日任务、测试、评估、文档和最终演示都必须持续为它们服务，不能等到项目最后再临时包装。

#### 2.0.1 必须凸显用户真正理解 Agent 工程与优化

- 项目最终不能只证明“代码能运行”或“调用了 Agent 框架”，还必须用架构说明、关键轨迹、评估结果、独立讲解和换业务场景迁移，证明用户真正理解 Agent 工程与优化。
- 用户必须能够独立解释目标、状态、动作、Observation、Agent Loop、工具选择、计划修订、上下文工程、Memory、RAG Evidence、错误恢复、终止条件、人机协同、安全边界、可观测性和评估之间的关系。
- 必须明确区分“LLM 负责的不确定性推理”和“确定性后端负责的权限、策略、事务与安全门禁”，不能把普通后端流程、固定工作流或框架 API 调用包装成 Agent 能力。
- 每个涉及 Agent 的 Day 都必须形成“原理 → 架构位置 → 关键实现 → 可观察运行轨迹 → 评估证据 → 换场景迁移验证”的证据链；框架内部替用户解决了什么也必须讲清楚。
- Agent 优化必须由失败样本、轨迹归因、基线对比和回归评估驱动。不能只凭主观感觉修改 Prompt，也不能用新增技术名词冒充优化。
- 最终答辩时，用户需要能在更换业务场景但底层原理相同的情况下，独立说明设计原因、替代方案、失败边界和优化方法。

#### 2.0.2 最终必须有具体数据体现项目价值

- 项目最终必须产出可复现的量化价值报告，使用具体数据说明 ResolveFlow 相比明确基线解决了什么问题、改善了什么指标、代价是什么。
- 指标至少应覆盖与业务和 Agent 质量直接相关的维度，例如任务完成率、正确处置率、工具选择与参数正确率、策略违规率、人工介入率、平均处理步骤或时间、P50/P95 延迟、Token 与成本；具体保留哪些指标由业务目标和评估证据决定。
- 每个数值都必须能够追溯到版本化数据集、测试或评估脚本、运行配置、样本量、原始结果和报告。合成数据必须显式标注，不能写成真实生产收益。
- 必须建立至少一个可重复执行的基线，并使用相同数据、相同判定标准和可比运行环境与最终版本对比；不能只展示优化后结果。
- 从项目早期就要设计埋点和证据采集，逐 Day 保存可用于最终价值评估的数据。不能到 Day 29 再根据印象补数字。
- 如果某个业务价值当前无法真实测量，必须明确写成“尚未验证”或“实验环境结果”，不得把估算、推测或单次演示写成确定收益。

### 2.1 解决真实问题

- 项目必须解决企业售后工单调查与处置中的真实流程问题，不能只做一个聊天演示。
- 必须围绕工单受理、证据调查、业务判断、风险审批、动作执行和结果验证形成闭环。
- 可以使用合成数据保证项目可复现，但必须明确说明数据是合成的，不能把模拟结果伪装成真实生产收益。
- 所有简历指标、成功率、延迟、成本和效果提升必须来自真实测试脚本、运行环境和报告；不得编造或把估算值写成实测值。

### 2.2 具备完整后端工程体系

- 项目不能只有 Prompt、Notebook 或单文件 Demo。
- 后端主线应覆盖 API、参数校验、业务分层、数据库、迁移、事务、认证授权、异步任务、幂等、错误处理、配置、测试、部署与故障恢复。
- 必须明确协议层、应用服务层、领域规则、Agent Runtime、工具适配层和基础设施层的职责边界。
- 涉及资金、权限、安全和数据一致性的最终判断必须由确定性后端代码执行，不能只依赖 Prompt 或模型自觉。

### 2.3 体现 Agent 真正的核心能力

- Agent 必须具有明确的目标、状态、动作、Observation、验证和终止条件。
- 必须能够根据工具返回结果动态改变下一步，而不是用固定工作流冒充 Agent。
- 应体现任务规划、工具选择、结构化参数、状态更新、错误恢复、结果验证和必要的计划修订。
- 普通 RAG 只能作为 Agent 的一个能力，不能把“检索一次再回答”当成完整 Agent。
- 优先直接使用适合生产项目的主流框架；使用框架后，必须向用户解释框架内部解决了哪些状态管理、流程控制、暂停恢复或工具编排问题。
- 不为展示名词盲目引入多 Agent、RL、微调或其他复杂技术。只有业务需求和评估证据证明有价值时才引入。

### 2.4 重视上下文工程

- 必须区分 History、State、Memory、RAG Evidence 和本轮 Context。
- Context 不能等同于完整聊天历史，也不能无脑拼接所有内容。
- 必须考虑信息来源、优先级、可信度、时效性、权限、Token 预算、压缩和丢弃理由。
- 系统安全规则、当前目标、完成条件、已确认事实和用户最新修改必须优先保留。
- 主事实发生改变时，所有依赖旧事实形成的派生状态、证据、计划和审批必须失效或重新验证。
- 外部文档和工具结果属于数据，不能覆盖系统安全规则。

### 2.5 具备可观测性和评估体系

- 每次 Agent Run 应能追踪 API、Worker、LLM、检索、工具、数据库、状态转移和人工审批。
- 必须记录必要的模型版本、Prompt 版本、工具版本、耗时、Token、成本、重试、错误和终止原因，同时对敏感信息脱敏。
- 评估不能只检查最终回答，还要检查任务结果、工具选择、工具参数、执行轨迹、引用、策略合规、安全、延迟和成本。
- 优先使用确定性规则评估可确定的事实；LLM Judge 只补充评估难以规则化的语义维度，并需要人工样本校准。
- 必须构建可版本化的 Golden Dataset、失败归因和回归评估流程。

### 2.6 具备人机协同

- 退款、补偿和其他高风险写操作必须支持持久化暂停、人工审批、拒绝、修改、恢复和接管。
- 人工审批和业务资格是两道独立门槛；人工批准不能绕过系统硬规则。
- 审批必须绑定具体工具、资源、参数和提案版本；参数发生变化后必须重新审批。
- 写操作必须具备权限校验、幂等键、审计记录和执行后验证。
- 信息不足、证据冲突、权限不足或系统不确定时，Agent 应追问、暂停或转人工，不能强行完成。

## 3. 角色与教学方式

### 3.1 固定角色

- 每个 Day 的任务进行过程中，Codex 始终扮演老师和主要开发者。
- 主要代码由 Codex 实现，但必须保证用户掌握项目的底层架构、核心原理和关键代码。
- 项目不再设置面试官角色，不进行逐题提问、掌握概率判断或换场景复问。

### 3.2 教学重点

- 用户当前主要目标是理解原理、底层架构和知识之间的联系，不是训练大量手写代码。
- 讲解新模块时，先说明业务问题、完整操作流程、核心原理和架构位置，再进入实现。
- 解释原理和框架时只在必要情况下使用代码示例，不能让大量代码掩盖原理。
- 知识依据以官方文档和必要的源码为主，但必须转化成直接、通俗、不绕圈子的中文。
- 讲解一个原理时，应同时串联与它紧密相关的状态、数据流、失败路径和上下游组件；不要把相关知识拆成互不相干的术语。
- 如果用户的理解、架构设想或技术判断存在问题，必须直接指出，并使用原理、代码行为、测试结果或工程事实说明；不得为了迎合用户而附和错误结论。

## 4. 代码实现与解释规则

- Codex 负责主要代码实现，用户重点掌握关键代码而不是逐行手写全部功能。
- 任何对理解核心流程有重要作用的代码，在出现后必须紧接着解释，不能先堆积大量代码后统一说明。
- 关键代码按照以下顺序讲解：
  1. 输入和输出；
  2. 实际执行流程；
  3. 为什么采用这种写法；
  4. 它在整体架构中的位置；
  5. 它造成的状态变化；
  6. 异常和失败如何处理。
- AgentState、工具契约、节点跳转、checkpoint、人工中断与恢复等 Agent 核心代码必须重点讲解；事务、权限、幂等、数据库和普通后端异常处理等相关后端代码直接解释，不进行提问验收。
- 不要求把格式检查、Lint 或静态类型检查作为每日完成的硬门槛；可以按工程需要使用，但不得替代自动化测试。

## 5. 每个 Day 的固定工作流

每个 Day 是按任务量定义的完整学习单元，与自然日期和实际耗时无关。不得因为已经学习 6 小时就把未完成的任务算作完成。

### 5.1 开始阶段

1. 明确当天要解决的业务问题。
2. 明确当天需要掌握的底层原理和框架能力。
3. 明确代码增量、测试范围、文档产物和完成标准。
4. 回顾上一个 Day 遗留的关键薄弱点，并在讲解中补充。

### 5.2 教学与实现阶段

1. 先按操作流程讲解原理和架构。
2. 由 Codex 完成主要代码实现。
3. 关键代码出现后立即按固定顺序解释。
4. 运行功能并收集可验证证据。
5. 运行自动化测试，覆盖正常路径、最重要的失败路径和边界情况。
6. 自动化测试未通过时不得完成当天任务。

### 5.3 原理总结文件阶段

每完成一个 Day，必须在项目根目录的 `原理总结/` 文件夹中，为该 Day 新建或更新一份独立 Markdown 总结，命名格式为 `dayXX_<topic>.md`。

总结必须简明扼要但内容完整，按照真实操作流程组织，不得只罗列新增文件或技术名词。总结至少包括：

1. 请求或输入从哪里进入；
2. 各层分别负责什么；
3. 数据和状态怎样变化；
4. 外部依赖或工具怎样调用；
5. 正常结果怎样返回和持久化；
6. 失败、重试、回滚、恢复怎样处理；
7. 关键设计为什么这样选择；
8. 相关原理之间怎样连接；
9. 对应的测试和运行证据。

完整总结只写入 `原理总结/` 中的对应文件，不在当前项目运行对话窗口中重复粘贴整篇内容。对话窗口只简短报告：Day 是否完成、关键结果、测试结果和总结文件链接。详细原理、关键代码与完整测试证据仍保留在 `docs/learning/dayXX_<topic>/` 中，`原理总结/` 用于快速复习和串联流程。

### 5.5 完成与推进

一个 Day 只有同时满足以下条件才算完成：

1. 当天功能已经真实运行；
2. 自动化测试通过，并覆盖正常、失败和边界路径；
3. 当天拆分文档已经完成；
4. `原理总结/` 中已经生成该 Day 的简明完整总结；
5. 仍需继续学习的非阻塞问题已经记录到后续范围或文档边界中。
当前 Day 通过后可以自动进入下一 Day，不需要重复请求许可。但以下情况禁止自动推进：
- 用户明确说“暂停”；
- 当前 Day 任何完成条件未满足；
- 下一步需要改变架构、技术栈或既定路线但尚未得到用户明确同意；
- 下一步需要执行本文件规定必须审批的操作。

## 6. 每日文档结构

- 每个 Day 建立独立目录，建议位置为 `docs/learning/dayXX_<topic>/`。
- 每个 Day 至少拆分以下文档，不把所有内容混在一个文件中：
  - `01_principles.md`：按操作流程整理的原理、底层机制和知识关联；
  - `02_key_code.md`：关键代码位置，以及输入输出、执行流程、写法原因、架构位置、状态变化和异常处理；
  - `03_test_evidence.md`：测试范围、运行命令、正常/失败/边界案例和真实结果；
- 每个完成的 Day 还必须在项目根目录 `原理总结/` 中建立一份独立快速复习文件；它不能替代上述详细文档。
- 每个关键指标都应能从测试证据文档追溯到脚本、配置、环境和原始报告。

## 7. 自动化测试要求

- 自动化测试是每个 Day 的硬性验收门槛。
- 测试至少覆盖当天正常成功路径、最重要的失败路径和关键边界条件。
- 对高风险写操作，必须测试未审批、审批参数不匹配、重复请求、部分成功和恢复后的行为。
- 对 Agent 流程，既要验证最终状态，也要验证关键轨迹、工具选择、参数和策略合规性。
- 不得用“手动试过一次”代替可重复的自动化测试。

## 8. 操作权限与变更边界

在平台安全规则允许的前提下：

- Codex 可以在 `resolveflow_agent` 目录内自主创建和修改非敏感项目文件、安装项目依赖、运行测试、启动项目所需的 Docker 服务和修改项目本地开发数据库。
- 修改 `resolveflow_agent` 目录以外的任何文件前，必须获得用户明确同意。
- 删除任何项目文件前必须获得用户明确同意。
- 清空或破坏性修改任何数据库数据前必须获得用户明确同意。
- 创建 Git 提交前必须获得用户明确同意。
- 推送远程仓库或创建 PR 前必须获得用户明确同意。
- 不得使用破坏性 Git 命令覆盖用户已有修改。
- 用户的“暂停”指令优先级高于自动推进规则；暂停后只能回答问题、整理已有内容或执行用户新明确授权的任务。

## 9. 明确禁止的行为

- 禁止只写代码不讲原理。
- 禁止为了迎合用户而不指出错误。
- 禁止跳过功能、测试、详细文档或原理总结擅自推进。
- 禁止用固定流程或单次 RAG 冒充真正的 Agent 能力。
- 禁止只展示成功路径而忽略异常、权限、安全和恢复。
- 禁止使用未经真实测试支持的效果数字。
- 禁止为了堆技术名词引入无法解释、无法评估或没有业务价值的复杂组件。
